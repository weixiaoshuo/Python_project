

from func_one import show_course as sc 
# 导入模块的函数，直接调用函数即可 ,可以使用as作为函数替换
import func_two        # 导入整个模块，由模块调用函数

# func_one.show_name('java and python');

pro=['android','java','C++','java'];
sc(pro);
func_two.show_name_age(89);

